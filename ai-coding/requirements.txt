# AI Coding 依赖

# 基础依赖
pyyaml>=6.0
gitpython>=3.1.0
requests>=2.28.0

# LLM 提供商 SDK（按需安装）
anthropic>=0.18.0        # Anthropic Claude
openai>=1.0.0            # OpenAI GPT
dashscope>=1.10.0        # 阿里通义千问
# deepseek, kimi, minmax, glm 使用 requests，无需额外安装